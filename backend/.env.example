# Database Configuration
DATABASE_URL=sqlite:///./price_tracker.db
# For PostgreSQL: postgresql://user:password@localhost/price_tracker

# OpenAI Configuration (for AI similarity features)
OPENAI_API_KEY=your_openai_api_key_here

# Email Notification Configuration
# SMTP settings (example for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
FROM_EMAIL=your_email@gmail.com
NOTIFICATION_EMAIL=recipient@example.com

# For Gmail: Use App Password (not regular password)
# Enable 2FA, then generate app password at: https://myaccount.google.com/apppasswords

# Alternative SMTP Providers:
# - SendGrid: smtp.sendgrid.net, port 587
# - AWS SES: email-smtp.us-east-1.amazonaws.com, port 587
# - Mailgun: smtp.mailgun.org, port 587
